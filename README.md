# product-catalog-shop
Antigravity project: Build an e-commerce product catalog with shopping cart. Requirements:**Frontend (React + Vite — must be buildable with npm run build):**- Product listing page: responsive grid of product cards (image placeholder, name, price, rating stars, Add to Cart button)- Product detail page: larger image, full description, quantity select
